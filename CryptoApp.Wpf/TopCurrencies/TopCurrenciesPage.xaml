<Page x:Class="CryptoApp.Wpf.TopCurrencies.TopCurrenciesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:CryptoApp.Wpf.TopCurrencies"
      mc:Ignorable="d"
      Title="TopCurrenciesPage" Height="450" Width="800"
      Loaded="Page_Loaded">
    <Grid>
        <TextBox Text="Top 10 Currencies"></TextBox>
        <ListBox ItemsSource="{Binding CryptoCurrencies}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="10 5"
                                Orientation="Horizontal">
                        <TextBox Text="{Binding Path=Name}" />
                        <TextBox Text="{Binding Path=PriceUsd}" />
                        <TextBox Text="{Binding Path=VolumeUsd24Hr}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>
